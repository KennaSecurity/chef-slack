task :foodcritic do
  system('bundle exec foodcritic ./')
end

desc "Linting"
task :foodcritic do
  system('bundle exec foodcritic ./')
end

desc "ChefSpec Unit tests"
task :spec do
  system('bundle exec rspec')
end

task default: [:foodcritic, :spec]

